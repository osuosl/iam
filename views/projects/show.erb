<div class="container">
  <h2>Project: <%= @project.name %> -
    <a href="/projects/<%= @project.id %>/edit">edit</a>
  </h2>
  <p>
    <% if @client.active %>
      Client:
        <a href="/clients/<%= @client.id %>" >
          <%= @client.name %>
        </a>
    <% end %>

    <br>
    Description: <%= @project.description %> <br>
    <%= @project.active ? "Active" : "Inactive" %><br>

    <% if @data.nil? %>
      <p>
        <%= @project.name %> has no resources
      </p>
    <% else %>
      <% Report.plugin_matrix_with_db.each do |resource , measurements| %>
        <table class="table">
          <thead>
            <h4><%= "#{resource}".capitalize %></h4>
            <tr>
              <% measurements.each do |meas| %>
                <th><%= "#{meas}" %></th>
              <% end %>
            </tr>
          </thead>
          <tbody>
            <tr>
              <% @data[resource].each do |resource_hash| %>
                <% resource_hash.each do |resource_meas| %>
                  <% resource_meas[1].each do |r_meas| %>
                    <td><%= r_meas[1] %></td>
                  <% end %>
                <% end %>
              <% end %>
            </tr>
          </tbody>
        </table>
      <% end %>
    <% end %>
  </p>
</div>
